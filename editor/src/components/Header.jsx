
import { AppBar, Toolbar, styled } from "@mui/material"

const Container = styled(AppBar)`
    background: #060606;
    position: static;
    height: 2vh;
`;

const Header = () => {
    
    return (
        <Container>
            
        </Container>
    )
}

export default Header;